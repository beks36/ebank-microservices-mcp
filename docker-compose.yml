version: '3.8'

services:
  consul:
    image: hashicorp/consul:1.19
    container_name: ebank-consul
    ports:
      - "8500:8500"   # UI web Consul : http://localhost:8500
      - "8600:8600/udp"
    command: "agent -dev -client=0.0.0.0"
    restart: unless-stopped
